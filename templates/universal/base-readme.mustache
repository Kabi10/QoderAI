{{!-- @title: Base README Template --}}
{{!-- @description: Universal README template for all project types --}}
{{!-- @category: universal --}}
{{!-- @outputPath: README.md --}}

# {{projectName}}

{{#description}}{{description}}{{/description}}{{^description}}A {{category}} project generated with Qoder's Universal Prompt Generator.{{/description}}

## Overview

This project was generated using the Qoder Universal Prompt Generator for the **{{category}}** category. It includes a complete, production-ready setup with modern development practices and tools.

### Key Features

{{#featureFlags}}
- ✅ {{.}}
{{/featureFlags}}
{{^featureFlags}}
- 🚀 Modern architecture and best practices
- 🔧 Complete development environment setup
- 📱 Responsive design (for web applications)
- 🔒 Security-first approach
- 📊 Built-in monitoring and analytics
- 🧪 Comprehensive testing suite
{{/featureFlags}}

### Technology Stack

{{#techStack}}
- **{{.}}**
{{/techStack}}
{{^techStack}}
- Modern technology stack optimized for {{category}} development
{{/techStack}}

### Target Audience

**{{targetAudience}}**

### Deployment Target

**{{deploymentTarget}}**

## Quick Start

### Prerequisites

{{#utils.includes techStack 'Node.js'}}
- Node.js 18+ and npm/yarn
{{/utils.includes}}
{{#utils.includes techStack 'Python'}}
- Python 3.8+ and pip
{{/utils.includes}}
{{#utils.includes techStack 'Java'}}
- Java 11+ and Maven/Gradle
{{/utils.includes}}
{{#utils.includes techStack 'Docker'}}
- Docker and Docker Compose
{{/utils.includes}}

### Installation

1. **Clone the repository** (if using git)
   ```bash
   git clone <repository-url>
   cd {{utils.kebabCase projectName}}
   ```

2. **Install dependencies**
   {{#utils.includes techStack 'Node.js'}}
   ```bash
   npm install
   # or
   yarn install
   ```
   {{/utils.includes}}
   {{#utils.includes techStack 'Python'}}
   ```bash
   pip install -r requirements.txt
   # or with virtual environment
   python -m venv venv
   source venv/bin/activate  # On Windows: venv\Scripts\activate
   pip install -r requirements.txt
   ```
   {{/utils.includes}}

3. **Configure environment**
   ```bash
   cp .env.example .env
   # Edit .env with your configuration
   ```

4. **Start development server**
   {{#utils.includes techStack 'React'}}
   ```bash
   npm start
   ```
   {{/utils.includes}}
   {{#utils.includes techStack 'Vue'}}
   ```bash
   npm run serve
   ```
   {{/utils.includes}}
   {{#utils.includes techStack 'Angular'}}
   ```bash
   ng serve
   ```
   {{/utils.includes}}
   {{#utils.includes techStack 'Express'}}
   ```bash
   npm run dev
   ```
   {{/utils.includes}}
   {{#utils.includes techStack 'Python'}}
   ```bash
   python app.py
   # or
   flask run
   ```
   {{/utils.includes}}

5. **Open your browser**
   {{#utils.includes category 'web-app'}}
   Navigate to [http://localhost:3000](http://localhost:3000)
   {{/utils.includes}}
   {{#utils.includes category 'api'}}
   API available at [http://localhost:3000](http://localhost:3000)
   API documentation at [http://localhost:3000/api/docs](http://localhost:3000/api/docs)
   {{/utils.includes}}

## Project Structure

```
{{utils.kebabCase projectName}}/
├── src/                    # Source code
{{#utils.includes category 'web-app'}}
│   ├── components/         # Reusable components
│   ├── pages/             # Page components
│   ├── hooks/             # Custom hooks
│   ├── utils/             # Utility functions
│   └── styles/            # CSS/SCSS files
{{/utils.includes}}
{{#utils.includes category 'api'}}
│   ├── routes/            # API routes
│   ├── models/            # Data models
│   ├── middleware/        # Express middleware
│   ├── controllers/       # Route controllers
│   └── utils/             # Utility functions
{{/utils.includes}}
├── public/                # Static assets
├── docs/                  # Documentation
├── tests/                 # Test files
├── config/                # Configuration files
{{#utils.includes techStack 'Docker'}}
├── docker/                # Docker configuration
├── Dockerfile             # Docker image definition
├── docker-compose.yml     # Multi-container setup
{{/utils.includes}}
├── .env.example           # Environment variables template
├── package.json           # Dependencies and scripts
└── README.md              # This file
```

## Development

### Available Scripts

{{#utils.includes techStack 'Node.js'}}
- `npm start` - Start development server
- `npm run build` - Build for production
- `npm test` - Run test suite
- `npm run test:watch` - Run tests in watch mode
- `npm run test:coverage` - Generate coverage report
- `npm run lint` - Run linter
- `npm run lint:fix` - Fix linting issues
{{/utils.includes}}

### Environment Variables

Copy `.env.example` to `.env` and configure the following variables:

```env
# Application
NODE_ENV=development
PORT=3000
APP_NAME={{projectName}}

{{#utils.includes featureFlags 'database'}}
# Database
DATABASE_URL=your_database_url
{{/utils.includes}}

{{#utils.includes featureFlags 'auth'}}
# Authentication
JWT_SECRET=your_jwt_secret
{{/utils.includes}}

{{#utils.includes featureFlags 'api-integration'}}
# External APIs
API_KEY=your_api_key
API_BASE_URL=https://api.example.com
{{/utils.includes}}
```

### Code Style and Linting

This project uses:
- **ESLint** for code linting
- **Prettier** for code formatting
- **Husky** for git hooks (pre-commit linting)

### Testing

{{#utils.includes featureFlags 'testing'}}
- **Unit Tests**: `npm test`
- **Integration Tests**: `npm run test:integration`
- **E2E Tests**: `npm run test:e2e`
- **Coverage**: `npm run test:coverage`
{{/utils.includes}}
{{^utils.includes featureFlags 'testing'}}
Run tests with:
```bash
npm test
```
{{/utils.includes}}

## Deployment

### Production Build

{{#utils.includes techStack 'Node.js'}}
```bash
npm run build
npm start
```
{{/utils.includes}}

### Docker Deployment

{{#utils.includes techStack 'Docker'}}
```bash
# Build image
docker build -t {{utils.kebabCase projectName}} .

# Run container
docker run -p 3000:3000 {{utils.kebabCase projectName}}

# Using Docker Compose
docker-compose up
```
{{/utils.includes}}

### Platform-Specific Deployment

{{#utils.includes deploymentTarget 'Vercel'}}
#### Vercel
```bash
npx vercel --prod
```
{{/utils.includes}}

{{#utils.includes deploymentTarget 'Netlify'}}
#### Netlify
```bash
npm run build
# Upload dist/ folder to Netlify
```
{{/utils.includes}}

{{#utils.includes deploymentTarget 'Heroku'}}
#### Heroku
```bash
heroku create {{utils.kebabCase projectName}}
git push heroku main
```
{{/utils.includes}}

## API Documentation

{{#utils.includes category 'api'}}
API documentation is available at `/api/docs` when running the development server.

### Key Endpoints

- `GET /health` - Health check
- `POST /api/auth/login` - User authentication
- `GET /api/users` - Get users
- `POST /api/users` - Create user

Full API specification follows OpenAPI 3.0 standards.
{{/utils.includes}}

## Configuration

### Constraints and Requirements

{{#constraints}}
- {{.}}
{{/constraints}}
{{^constraints}}
- Follow semantic versioning
- Maintain backwards compatibility
- Ensure responsive design
- Implement proper error handling
- Include comprehensive logging
{{/constraints}}

## Troubleshooting

### Common Issues

1. **Port already in use**
   ```bash
   # Kill process on port 3000
   npx kill-port 3000
   ```

2. **Node modules issues**
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

3. **Environment variables not loading**
   - Ensure `.env` file exists in project root
   - Check variable names match `.env.example`
   - Restart development server

### Getting Help

- Check the [documentation](./docs/)
- Review [issues](./docs/troubleshooting.md)
- Contact the development team

## Contributing

1. Fork the repository
2. Create a feature branch (`git checkout -b feature/amazing-feature`)
3. Commit your changes (`git commit -m 'Add amazing feature'`)
4. Push to the branch (`git push origin feature/amazing-feature`)
5. Open a Pull Request

### Development Guidelines

- Follow the existing code style
- Write tests for new features
- Update documentation as needed
- Ensure all tests pass before submitting PR

## License

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.

## Acknowledgments

- Generated with [Qoder Universal Prompt Generator](https://github.com/qoder/prompt-generator)
- Built for **{{targetAudience}}**
- Optimized for **{{deploymentTarget}}** deployment

---

**Generated on:** {{date.iso}}  
**Category:** {{category}}  
**Version:** 1.0.0